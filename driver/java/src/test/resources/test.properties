# configurations for standalone
single.host=${single.host}
single.ip=${single.ip}
single.port=${single.port}
single.username=${single.username}
single.password=${single.password}

# configurations for cluster
cluster.urls=${cluster.urls}
cluster.username=${cluster.username}
cluster.password=${cluster.password}
dbPath=${dbpath}}
coord.host=${coord.host}
coord.port=${coord.port}
coord.path=${coord.path}
data.group=${data.group}
data.host=${data.host}
data.port=${data.port}
# A coord address of another cluster
datasource.address=${coord.address}
# Multiple coord addresses of other clusters, spearated by ','
datasource.urls=${coord.urls}

# enable '--privilegecheck' sdb
rbac.root.username=${rbac.root.username}
rbac.root.password=${rbac.root.password}
rbac.coord.host=${rbac.coord.host}
rbac.coord.port=${rbac.coord.port}
rbac.newNode.dbPathPrefix=${rbac.newNode.dbPathPrefix}